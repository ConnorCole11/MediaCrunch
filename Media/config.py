# Enter absolute paths to:
class config:
    def __init__(self):
        self.playlist_folder = '...' # the folder with the playlist text files
        self.video_folder = '...' # the folder that holds videos
        self.song_folder = '...' # the folder that holds songs (If you have a parent songFolder with a subfolder for a group of songs like a game ost, use the parent folder.)
        self.audio_folder = '...' # the folder that holds audio clips
        self.image_folder = '...' # the folder that holds images
        self.gif_folder = '...' # the folder that holds gifs